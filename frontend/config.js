// Frontend-only deployment config.
// Set this to your backend URL before deploying (or override it at runtime).
// Example: window.__API_BASE__ = "https://your-backend-domain.com";
window.__API_BASE__ = "http://127.0.0.1:8000";

